spring:
  application:
    name: projeto-escola
  datasource:
    url: jdbc:postgresql://localhost:5432/projeto_escola
    username: usergabs
    password: mysecretpassword
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: update